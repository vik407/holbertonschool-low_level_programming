#include <stdio.h>

/**
 * main - prints the alphabet in lowercase,
 * followed by a new line.
 * Return: 0
 */

int main(void)
{
	char alphabeth[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = 1; i <= 26; ++i)
	{
		putchar(alphabeth[i]);
	}
	return (0);
}
