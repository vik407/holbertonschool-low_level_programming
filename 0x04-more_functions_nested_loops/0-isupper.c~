#include "holberton.h"
#include <stdio.h>
#include <ctype.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The character to check
 *
 * Return: On true 1.
 * On false 0
 */

int _isupper(int c)
{
	_putchar(isupper(c));
}
